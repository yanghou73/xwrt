# ============================================================
# 精简模式片段 — 禁用非必要服务，提升运行速度
# 适用：RAM 受限设备（如 WR30U 256MB）
# 保留：OpenClash、基础路由、LuCI 管理、SSH
# 禁用：SmartDNS、iStore、Samba4、Lucky、重型开发工具
# 拼接顺序：在 general.txt / devtools.txt 之后，确保覆盖
# ============================================================

# --- SmartDNS ---
# OpenClash 内置 DNS 处理，无需额外 DNS 服务
CONFIG_PACKAGE_luci-app-smartdns=n
CONFIG_PACKAGE_smartdns=n

# --- Lucky（端口转发/DDNS，运行不稳定）---
CONFIG_PACKAGE_luci-app-lucky=n

# --- iStore 应用商店 ---
# 后台守护进程，占用 RAM，可后续按需 opkg 安装
CONFIG_PACKAGE_luci-app-istore=n

# --- 网络测速 ---
# 依赖 Python3，占用存储空间
CONFIG_PACKAGE_luci-app-netspeedtest=n

# --- Samba4 文件共享 ---
# 重型服务，路由器不做 NAS 时不需要
CONFIG_PACKAGE_luci-app-samba4=n

# --- Coremark（CPU 基准测试）---
# 持续占用 CPU，无实际用途
CONFIG_PACKAGE_coremark=n

# --- ttyd（Web 终端）---
# 用 SSH 替代，节省 RAM
CONFIG_PACKAGE_luci-app-ttyd=n
CONFIG_PACKAGE_ttyd=n

# --- Gecoosac ---
# 非必要 LuCI 插件
CONFIG_PACKAGE_luci-app-gecoosac=n

# --- WolPlus ---
# 基础 WoL（luci-app-wol）已足够
CONFIG_PACKAGE_luci-app-wolplus=n

# --- 重型开发工具（保留 bash/ssh/curl/nano/htop，其余靠 busybox）---
CONFIG_PACKAGE_strace=n
CONFIG_PACKAGE_tcpdump=n
CONFIG_PACKAGE_mtr-nojson=n
CONFIG_PACKAGE_socat=n
CONFIG_PACKAGE_iperf3=n
CONFIG_PACKAGE_screen=n
CONFIG_PACKAGE_less=n
CONFIG_PACKAGE_tree=n
CONFIG_PACKAGE_file=n
CONFIG_PACKAGE_rsync=n
CONFIG_PACKAGE_diffutils=n
CONFIG_PACKAGE_patch=n
CONFIG_PACKAGE_unzip=n
CONFIG_PACKAGE_zip=n
CONFIG_PACKAGE_lsof=n
CONFIG_PACKAGE_procps-ng=n
CONFIG_PACKAGE_coreutils=n
CONFIG_PACKAGE_findutils-find=n
CONFIG_PACKAGE_findutils-xargs=n
CONFIG_PACKAGE_xz-utils=n
CONFIG_PACKAGE_wget-ssl=n
