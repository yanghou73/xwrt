# ============================================================
# x64 HomeProxy 代理面板（ImmortalWrt 官方，基于 sing-box）
# 源码：immortalwrt/homeproxy
# 验证状态：⏳ 待验证
# 注意：依赖 sing-box，可能存在 Go 版本兼容性问题
# ============================================================

# --- HomeProxy LuCI 界面 ---
CONFIG_PACKAGE_luci-app-homeproxy=y

# --- 核心依赖（sing-box 自动被依赖拉入） ---
# firewall4 / nftables 由 lede 默认提供
# kmod-nft-tproxy 在 general.txt 中已包含
# ucode-mod-digest 由 homeproxy Makefile 自动依赖

# --- 注意事项 ---
# 1. homeproxy 强依赖 sing-box，Go 版本不兼容时会编译失败
# 2. 如编译失败，可尝试关闭此选项
# 3. 与 OpenClash / PassWall 同时使用可能端口冲突
